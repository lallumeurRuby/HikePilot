'use client'

/**
 * Top navigation bar component.
 * Worker should add user info, logout, etc. based on flow-map.
 */
export function TopBar() {
  return (
    <header data-testid="topbar" style={{ height: 56, borderBottom: '1px solid #eee', display: 'flex', alignItems: 'center', padding: '0 1rem' }}>
      {/* Worker fills content based on layout-specs */}
    </header>
  )
}
